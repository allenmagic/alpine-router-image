# deploy 通道：root/root 密码登录（guest 无状态，host key 每次启动更换，
# deploy 用 StrictHostKeyChecking=no 连接）。见 docs/refactor-proposal.md §3.3。
#
# 通道只在 LAN 侧（br-lan）可达；公钥注入后日常登录走 SSH_PUBLIC_KEY
# （authorized_keys），密码登录保留给自动 deploy（每次重启后 re-inject
# 的引导通道，无法依赖还没注入的公钥）。
PermitRootLogin yes
