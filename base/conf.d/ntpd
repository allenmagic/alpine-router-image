# busybox ntpd 上游：阿里云 + 腾讯 NTP 池（与 dnsmasq 上游 DNS 同源选择）
# 注：Alpine 链的 init 脚本名为 ntpd（读本文件）；Gentoo 链的
# busybox-ntpd 上游在 distros/gentoo/setup.sh 内联（同名源）
NTPD_OPTS="-N -p ntp.aliyun.com -p ntp.tencent.com"
