# ============================================================
# 上游 DNS 服务器配置
# 仅在 yunshu 容器故障、VRRP 切回本 VM 时作为 fallback DNS。
# ============================================================

# 不读取 /etc/resolv.conf，只使用下面显式上游，避免 fallback 行为漂移。
no-resolv

# 阿里云 DNS
server=223.5.5.5
server=223.6.6.6

# 腾讯 DNS
server=119.29.29.29
server=182.254.116.116
